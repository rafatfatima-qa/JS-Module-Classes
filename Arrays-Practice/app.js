let students = [2, 3, 12, 58, 3, 6, 4, 58, 25, 11, 0]; //use Shift + Alt + F On Windows for Indentation (of array aswell)


for (let i = 0; i < students.length; i++){

    console.log("Student# " + (i+1) + " marks:" +students[i]);
}